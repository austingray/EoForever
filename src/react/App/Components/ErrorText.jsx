import styled from 'styled-components';
import { $fontUIError } from '../utilities/fonts';
import { $red } from '../utilities/colors';

export default styled.div`
  font-size: 18px;
  margin-top: 4px;
  color: ${$red};
  font-family: ${$fontUIError};
`;
